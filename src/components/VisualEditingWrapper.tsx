'use client'

import { enableVisualEditing } from '@sanity/visual-editing'

export default function VisualEditingWrapper() {
  // Enable visual editing for the presentation tool
  enableVisualEditing()
  return null
}
